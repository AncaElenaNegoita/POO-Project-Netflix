package inputdata;

public interface PrototypeMovie {
    /**
     * Function implemented in MovieList that creates a copy of it
     * @return - the array that will be converted to MovieList
     */
    PrototypeMovie getClone();
}
